name: test

on: push

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: ${{ vars.dev }}
    steps:
      - name: Show Branch
        run: echo ${{ github.ref_name	}}
      - name: Show ENV Variables
        run: env | sort
